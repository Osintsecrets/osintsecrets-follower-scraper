# OSINTsecrets follower scraper

A GitHub Actions job fetches the public follower total for `@osintsecrets` every five minutes and publishes the latest verified value in `data/followers.json`.

No Instagram account credentials, access tokens, or private profile information are used or stored.
